let courseData = {
  Title : 'Java Script',
  Author : 'Hitesh Choudhary',
  Duration : '10hrs',
  Description : 'This course teaches the basics of java Script language',
  Price : '10$'
}

console.log(courseData);
console.log(`There is a new course available "${courseData.Title}" by "${courseData.Author}" of duration "${courseData.Duration}" with description "${courseData.Description}" at price "${courseData.Price}".`);
